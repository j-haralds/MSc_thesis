
data_pybamm.ipynb calls functions in sim_pybamm.py and exp_pybamm_ver4.py to generate voltage trajectory data.

The file can save data in the format, with compression and force dummy values, to match the data format used in the nn_model directory. It can also generate the Ue_run_pybamm.txt used in the nn_model directory
